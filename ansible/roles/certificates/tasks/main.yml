---

- name: ensure logfile exists
  file: path=/var/log/segue/certificates.log
        owner=www-data
        mode=0777
        state=touch

- name: ensure template folder exists
  file: path={{ app_path }}/segue/document/templates
        owner=www-data
        mode=0777
        state=directory

- name: copy template files to application template folder
  tags: certificates
  synchronize: src={{ project_dir }}/segue/document/templates/
               dest={{ app_path }}/segue/document/templates/
               rsync_opts=-O
               owner=no
               group=no
               mode=no
